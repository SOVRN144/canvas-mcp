# Comma-separated list of allowed origins for CORS (leave empty to disable browser origins)
CORS_ALLOW_ORIGINS=http://localhost:3000,https://your-connector.example

# Idle session TTL in milliseconds (default 600000 = 10 minutes)
SESSION_TTL_MS=600000
